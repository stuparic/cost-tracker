<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" width="512" height="512">
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#10b981;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#059669;stop-opacity:1" />
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="512" height="512" fill="url(#grad)"/>

  <!-- Wallet icon with safe zone padding (10% on all sides) -->
  <g transform="translate(256, 256) scale(0.7)">
    <!-- Wallet body -->
    <rect x="-120" y="-80" width="240" height="160" rx="20" fill="#ffffff" opacity="0.95"/>

    <!-- Wallet fold -->
    <path d="M -120 -40 Q -120 -60, -100 -60 L 100 -60 Q 120 -60, 120 -40 Z" fill="#ffffff" opacity="0.7"/>

    <!-- Card slot -->
    <rect x="-90" y="-20" width="180" height="80" rx="10" fill="none" stroke="#10b981" stroke-width="4"/>

    <!-- Currency symbols -->
    <text x="0" y="30" text-anchor="middle" font-family="Arial, sans-serif" font-size="48" font-weight="bold" fill="#10b981">€</text>
    <text x="-50" y="25" text-anchor="middle" font-family="Arial, sans-serif" font-size="32" font-weight="bold" fill="#059669" opacity="0.6">RSD</text>
  </g>
</svg>